'use strict';

describe('gameCanvas', function() {

  // Load the module that contains the `gameCanvas` component before each test`
  beforeEach(module('gameCanvas'));

  // Test the controller
  describe('GameCanvasController', function() {
    var $httpBackend, ctrl;
  })
})
